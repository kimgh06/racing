version: "3.9"

services:
  falcare:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: falcare
    environment:
      NODE_ENV: production
      PORT: 3000
    ports:
      - "3000:2313" # host:container
    restart: unless-stopped
